{"ast":null,"code":"import { BehaviorSubject, of } from 'rxjs';\nimport { HttpClient } from '@angular/common/http';\nimport { catchError, tap } from 'rxjs/operators';\nimport * as i0 from \"@angular/core\";\nimport * as i1 from \"@angular/common/http\";\nexport let TimesheetService = /*#__PURE__*/(() => {\n  class TimesheetService {\n    http;\n    baseUrl = 'http://localhost:3000/api';\n    normalizeEntry(entry) {\n      const hours = Number(entry.hours ?? 0);\n      const rate = Number(entry.payAmount ?? 0);\n      const totalPay = Number(entry.totalPay ?? hours * rate);\n      return {\n        ...entry,\n        hours,\n        payAmount: rate,\n        totalPay,\n        departmentId: entry.departmentId ?? ''\n      };\n    }\n    // local cache for departments (start empty)\n    departments = [];\n    // currently selected department\n    selectedDepartment;\n    selectedDepartment$ = new BehaviorSubject(undefined);\n    entriesSubject = new BehaviorSubject([]);\n    entries$ = this.entriesSubject.asObservable();\n    departmentsSubject = new BehaviorSubject(this.departments);\n    departments$ = this.departmentsSubject.asObservable();\n    constructor(http) {\n      this.http = http;\n    }\n    addEntry(entry) {\n      const normalized = this.normalizeEntry(entry);\n      return this.http.post(`${this.baseUrl}/entries`, normalized).pipe(tap(saved => {\n        const current = this.entriesSubject.getValue();\n        this.entriesSubject.next([this.normalizeEntry(saved), ...current]);\n      }), catchError(err => {\n        console.error('POST /entries failed, using local fallback', err);\n        const localEntry = this.normalizeEntry({\n          ...entry,\n          ...(entry._id ? {\n            _id: entry._id\n          } : {\n            _id: `local-${Date.now()}-${Math.random().toString(16).slice(2)}`\n          })\n        });\n        this._localAdd(localEntry);\n        const current = this.entriesSubject.getValue();\n        this.entriesSubject.next([localEntry, ...current]);\n        return of(localEntry);\n      }));\n    }\n    getAll() {\n      return this.http.get(`${this.baseUrl}/entries`).pipe(tap(list => this.entriesSubject.next(list.map(entry => this.normalizeEntry(entry)))), catchError(err => {\n        console.error('GET /entries failed, returning local cache', err);\n        const local = this._localGetAll().map(entry => this.normalizeEntry(entry));\n        this.entriesSubject.next(local);\n        return of(local);\n      }));\n    }\n    updateEntry(id, data) {\n      const normalized = this.normalizeEntry({\n        ...(this.entriesSubject.getValue().find(entry => entry._id === id || entry.id === id) || {}),\n        ...data,\n        totalPay: Number((Number(data.hours ?? 0) * Number(data.payAmount ?? 0)).toFixed(2))\n      });\n      return this.http.put(`${this.baseUrl}/entries/${id}`, normalized).pipe(tap(updated => {\n        const current = this.entriesSubject.getValue();\n        const item = this.normalizeEntry(updated);\n        this.entriesSubject.next(current.map(entry => {\n          const entryId = entry._id || entry.id;\n          return entryId === id ? item : entry;\n        }));\n        const localEntries = this._localGetAll();\n        this._entries = localEntries.map(entry => {\n          const entryId = entry._id || entry.id;\n          return entryId === id ? item : entry;\n        });\n      }), catchError(err => {\n        console.error('PUT /entries failed, applying local fallback', err);\n        const current = this.entriesSubject.getValue();\n        const item = this.normalizeEntry({\n          ...(current.find(entry => entry._id === id || entry.id === id) || {}),\n          ...data,\n          totalPay: Number((Number(data.hours ?? 0) * Number(data.payAmount ?? 0)).toFixed(2))\n        });\n        const updatedCurrent = current.map(entry => {\n          const entryId = entry._id || entry.id;\n          return entryId === id ? item : entry;\n        });\n        this.entriesSubject.next(updatedCurrent);\n        const localEntries = this._localGetAll();\n        this._entries = localEntries.map(entry => {\n          const entryId = entry._id || entry.id;\n          return entryId === id ? item : entry;\n        });\n        return of(item);\n      }));\n    }\n    deleteEntry(id) {\n      const key = id || '';\n      const removeFromLocal = () => {\n        const localEntries = this._localGetAll();\n        const updatedLocal = localEntries.filter(entry => {\n          const entryId = entry._id || entry.id;\n          return entryId !== key;\n        });\n        this._entries = updatedLocal;\n      };\n      return this.http.delete(`${this.baseUrl}/entries/${key}`).pipe(tap(() => {\n        const current = this.entriesSubject.getValue();\n        const updated = current.filter(entry => {\n          const entryId = entry._id || entry.id;\n          return entryId !== key;\n        });\n        this.entriesSubject.next(updated);\n        removeFromLocal();\n      }), catchError(err => {\n        console.error('DELETE /entries failed, applying local fallback', err);\n        const current = this.entriesSubject.getValue();\n        const updated = current.filter(entry => {\n          const entryId = entry._id || entry.id;\n          return entryId !== key;\n        });\n        this.entriesSubject.next(updated);\n        removeFromLocal();\n        return of(null);\n      }));\n    }\n    _localAdd(entry) {\n      const normalized = this.normalizeEntry(entry);\n      this._localGetAll().unshift(normalized);\n    }\n    _localGetAll() {\n      // ensure we have an in-memory array attached to service\n      if (!this._entries) this._entries = [];\n      return this._entries;\n    }\n    setSelectedDepartment(dept) {\n      this.selectedDepartment = dept;\n      this.selectedDepartment$.next(dept);\n    }\n    getSelectedDepartment() {\n      return this.selectedDepartment;\n    }\n    getDepartments() {\n      return this.http.get(`${this.baseUrl}/departments`).pipe(tap(list => this.departmentsSubject.next(list)), catchError(err => {\n        console.error('GET /departments failed, returning local list', err);\n        this.departmentsSubject.next(this.departments);\n        return of(this.departments);\n      }));\n    }\n    addDepartment(dept) {\n      return this.http.post(`${this.baseUrl}/departments`, dept).pipe(tap(saved => {\n        const current = this.departmentsSubject.getValue();\n        this.departmentsSubject.next([saved, ...current]);\n      }), catchError(err => {\n        console.error('POST /departments failed, using local fallback', err);\n        const local = {\n          _id: Math.random().toString(36).slice(2),\n          ...dept\n        };\n        const current = this.departmentsSubject.getValue();\n        this.departmentsSubject.next([local, ...current]);\n        return of(local);\n      }));\n    }\n    updateDepartment(id, data) {\n      return this.http.put(`${this.baseUrl}/departments/${id}`, data).pipe(tap(updated => {\n        const current = this.departmentsSubject.getValue();\n        this.departmentsSubject.next(current.map(d => d._id === updated._id ? updated : d));\n      }), catchError(err => {\n        console.error('PUT /departments failed', err);\n        return of(null);\n      }));\n    }\n    deleteDepartment(id) {\n      return this.http.delete(`${this.baseUrl}/departments/${id}`).pipe(tap(() => {\n        const current = this.departmentsSubject.getValue();\n        this.departmentsSubject.next(current.filter(d => (d._id || d.id) !== id));\n      }), catchError(err => {\n        console.error('DELETE /departments failed, applying local fallback', err);\n        // remove locally so UI responds even if backend is down\n        const current = this.departmentsSubject.getValue();\n        this.departmentsSubject.next(current.filter(d => (d._id || d.id) !== id));\n        return of(null);\n      }));\n    }\n    static ɵfac = function TimesheetService_Factory(__ngFactoryType__) {\n      return new (__ngFactoryType__ || TimesheetService)(i0.ɵɵinject(i1.HttpClient));\n    };\n    static ɵprov = /*@__PURE__*/i0.ɵɵdefineInjectable({\n      token: TimesheetService,\n      factory: TimesheetService.ɵfac,\n      providedIn: 'root'\n    });\n  }\n  return TimesheetService;\n})();","map":null,"metadata":{},"sourceType":"module","externalDependencies":[]}